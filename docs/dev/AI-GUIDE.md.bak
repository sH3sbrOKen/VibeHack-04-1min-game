# AI-GUIDE — AI协作规范

> 新对话开始时先读这份文档，再读 INDEX.md 找具体文件。

---

## 一、项目是什么

**游戏名**：今天不想回消息（备选：1% / 99+）
**类型**：1分钟手机竖屏休闲游戏，纯网页（单个 HTML 文件，无框架）
**核心场景**：手机1%电，99+条消息，玩家只能用四个预设短语回复，答错触发隐藏数值，数值满触发支线关卡解锁

**黑客松背景**：VibeFriends x 张江AI创新小镇 x Aseed+ / 24h Vibe Coding / 2026-05-30 上海现场

---

## 二、文件清单

| 文件 | 职责 | 修改频率 |
|------|------|--------|
| `INDEX.md` | 项目索引，每次更新文件都要同步更新 | 高 |
| `AI-GUIDE.md` | 本文件，AI协作规范 | 低 |
| `GDD.md` | 游戏设计文档，核心循环/数值/支线/文案 | 中 |
| `CONTENT.md` | 信息卡结构定义 + 26条信息卡数据 + 数值注册表 | 中 |
| `DEV-PLAN.md` | AI开发分工表 + Claude Code提示词模板 | 低 |
| `index.html` | 唯一可运行文件，包含全部游戏代码 | 高 |

---

## 三、index.html 内部结构

文件分三层，修改时只动对应层：

```
<style>   ← 视觉层：配色、布局、动画样式
<body>    ← 结构层：HTML骨架、按钮、弹窗
<script>  ← 逻辑层：游戏规则、数值、消息判断
```

**视觉改动**：只动 `<style>`，不碰 `<script>`
**内容改动**：只动 `<script>` 里的 `RAW` 数组
**规则改动**：只动 `<script>` 里的 `RULES` 数组和 `STAT_CFG`
**动画改动**：`@keyframes` 在 `<style>` 里，但命名被 JS 引用，改名要同步

---

## 四、核心数据结构速查

### 信息卡字段
```js
{ t: SenderType, n: '显示名', b: '正文', e: {StatKey: delta}, d: 1|2|3, trap?: true, easter?: true }
```

### 四个回复按钮
| ReplyType | 文字 | 位置 | 颜色 |
|-----------|------|------|------|
| WAIT | 等等 | 左上 | 橙 #FF9500 |
| RECEIVED | 收到 | 右上 | 蓝 #007AFF |
| UNSUBSCRIBE | 退订 | 左下 | 红 #FF3B30 |
| LOVE | 好的亲 | 右下 | 绿 #34C759 |

### 正确答案推导（优先级：正文规则 > 发信人默认）
```
RULES 数组逐条匹配正文 → 命中返回 override
未命中 → 返回 DEFAULTS[senderType]
```

### 六个数值
| StatKey | 初始 | 触发阈值 | 方向 |
|---------|------|--------|------|
| DELIVERY_LOST | 0 | ≥3 | 越高越坏 |
| BOSS_PATIENCE | 100 | ≤0 | 越低越坏 |
| INFO_LEAK | 0 | ≥3 | 越高越坏 |
| INTIMACY | 100 | ≤0 | 越低越坏 |
| SPAM_SUBSCRIBED | 0 | ≥5 | 越高越坏 |
| UNEXPECTED_CHARGE | 0 | ≥200 | 越高越坏 |

---

## 五、游戏流程

```
开始画面 → startGame()
  ↓
nextMsg() 弹出信息卡（难度1→2→3分阶段）
  ↓
reply(r) 玩家点击四角按钮
  ├─ 答对 → flashCorrect()，继续
  ├─ 答错 → 触发 StatEffect，飘字反馈
  └─ 回完99条 → showCongrats() → dismissCongrats() → endGame()
  ↓
60秒耗尽 → endGame()
  ↓
结算页：展示数值 + 解锁提示 + 分享文案
```

---

## 六、合作规范

### 开始新任务前必做
1. 读 `INDEX.md` 确认最新文件状态
2. 明确本次任务影响哪一层（视觉/内容/逻辑）
3. 说出修改计划，等用户确认再动

### 修改代码
- 只改任务涉及的最小范围，不顺带重构
- 视觉和逻辑分离，不交叉修改
- 每次改完说明改了哪里、为什么这样改

### 修改文档
- 改完 `index.html` 要同步更新 `INDEX.md` 的最后更新日期
- 游戏规则有重大变化时同步更新 `GDD.md`
- 新增信息卡时同步更新 `CONTENT.md`

### 新增功能
- 先描述方案（1-2句），等确认再写代码
- 新增 HTML 元素放在对应注释块附近
- 新增 CSS 放在对应模块注释下方
- 新增 JS 函数放在功能相关函数附近

### 遇到问题
- 同一问题尝试3次无进展：停下来，描述卡点，问用户
- 逻辑和视觉冲突：优先保逻辑，视觉妥协
- 不确定需求时：列出理解方式，让用户选

### 禁止事项
- 不重构运行正常的代码
- 不添加用户未要求的功能
- 不改动无关的 CSS 或 JS
- 不把单个 HTML 拆分成多文件（黑客松分发需要单文件）

---

## 七、当前进度

- [x] 游戏设计完成
- [x] 信息卡内容结构化（26条）
- [x] Demo 可运行（iOS视觉风格）
- [x] 恭喜弹窗（回完99条提前结算）
- [ ] 支线关卡实现
- [ ] 补充信息卡至42条
- [ ] 视觉打磨
- [ ] 黑客松展示准备
